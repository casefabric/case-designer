import ElementDefinition from "./elementdefinition";
import ModelDefinition from "./modeldefinition";

export default class ReferableElementDefinition<M extends ModelDefinition> extends ElementDefinition<M> {
}
